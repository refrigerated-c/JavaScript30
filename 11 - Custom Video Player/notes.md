input [value ="1"] - the initial value of an <input> element default content or state for the input before user interaction.

function scrubTime - 
1.e = event object , where u clicked, mouse position
2. e.offsetX = how many pixels from left side of progressBar u clicked
3. progress.offsetWidth = total width of progress bar in px
4. / divinding = gives the % of video
5. * video.duration = total length of video
     so if video is 100 seconds long  
     0.5 * 100 = 50 seconds